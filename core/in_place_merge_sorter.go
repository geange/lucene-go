package core

// InPlaceMergeSorter Sorter implementation based on the merge-sort algorithm that merges in place (no extra
// memory will be allocated). Small arrays are sorted with insertion sort.
type InPlaceMergeSorter interface {
}
